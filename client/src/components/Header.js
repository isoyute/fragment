import styles from '../styles/Header.module.scss';

const Header = () => {
    return (
        <div className={styles.header}>
            <h3>fragment</h3>
        </div>
    );
};

export default Header;
